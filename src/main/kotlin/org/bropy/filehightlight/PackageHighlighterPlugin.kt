package org.bropy.filehightlight

import com.intellij.openapi.components.Service
import com.intellij.openapi.project.Project

/**
 * Main plugin class for Package Highlighter.
 * Currently empty as we're using extension points for core functionality.
 * This class can be expanded for more complex features in the future.
 */
@Service
class PackageHighlighterPlugin(project: Project) {
    // This is a placeholder for future implementation
    // You might add plugin initialization code here if needed

    init {
        // Initialize any project-level components if needed
    }
}